name: 'Auto release draft'
description: 'Create a draft template '
author: 'Me'
outputs:
  release-url:
    descritpion: 'The URL of the github release that was drafted'
runs:
  using: 'node12'
  main: 'dist/index.js'
